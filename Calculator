<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>CGPA ⇆ Percentage</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap');

:root {
  --blue: #007BFF;
  --green: #28a745;
  --red: #dc3545;
}

* { box-sizing: border-box; }

body {
  font-family: 'Poppins', sans-serif;
  background: #fff;   /* ✅ White background */
  padding: 15px;
  margin: 0;
  color: #333;
  text-align: center;
}

header img {
  height: 100px; /* ✅ Reduced logo */
  margin-bottom: 5px;
  filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.4));
}
header h1 {
  margin: 5px 0;
  font-size: 28px;
  font-weight: bold;
  text-shadow: 2px 2px 6px rgba(0,0,0,0.4));
}

/* ✅ Calculator Box with Dynamic Border */
.calculator {
  background: #fff;
  padding: 25px;
  border-radius: 12px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.15);
  text-align: center;
  width: 330px;
  margin: 40px auto;
  border: 7px solid transparent; /* Default */
}

h2 {
  margin-bottom: 20px;
  color: #333;
}

input {
  width: 90%;
  padding: 10px;
  margin: 10px 0;
  border-radius: 8px;
  border: 1px solid #ccc;
  font-size: 16px;
  text-align: center;
}

.result {
  margin-top: 15px;
  font-weight: bold;
  font-size: 18px;
}

.blue { color: var(--blue); }
.green { color: var(--green); }
.error { color: var(--red); font-size: 14px; margin-top: 5px; }

footer {
  margin-top: 25px;
  font-weight: bold;
  color: #007bff;
}
</style>
</head>
<body>

  <div class="container">
    <div class="main-header">
      <header>
        <img src="https://i.ibb.co/nMC0877T/UNIVERSITY-UPDATES-LOGO-removebg-preview.png" alt="Calculator Logo">
        <h1>University Updates</h1>
      </header>
    </div>

<!DOCTYPE html>
<html lang="en">
  <style>
    a {
      text-decoration: none;
      font-size: 18px;
      color: #fff;
      background: #007BFF;
      padding: 10px 20px;
      border-radius: 8px;
      transition: background 0.3s;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }
    a:hover {
      background: #0056b3;
    }
    .icon {
      font-size: 20px;
    }
  </style>
<body>
  <a href="index.html">
    <span class="icon">🏠</span> Home
  </a>
</body>
</html>

    <!-- ✅ Calculator Box -->
    <div class="calculator" id="calculatorBox">
      <h2>CGPA ⇆ Percentage</h2>
      <input type="number" id="inputValue" placeholder="Enter CGPA or %" step="any" oninput="convertValue()">
      <div class="result" id="result"></div>
      <div class="error" id="error"></div>
    </div>
    
    <footer>
      © 2025 | Designed by <b>Aditya Chaudhary💗</b>
    </footer>
  </div>

<script>
function convertValue() {
  let value = parseFloat(document.getElementById("inputValue").value);
  let result = document.getElementById("result");
  let error = document.getElementById("error");
  let box = document.getElementById("calculatorBox");

  result.innerText = "";
  error.innerText = "";
  result.className = "result"; // reset classes
  box.style.borderColor = "transparent"; // reset border

  if (isNaN(value)) return;

  if (value <= 10) { // CGPA case
    if (value < 0) {
      error.innerText = "⚠️ CGPA cannot be negative";
      box.style.borderColor = "var(--red)";
      return;
    }
    let percentage = (value * 9.5).toFixed(2);
    result.innerText = `📘 CGPA = ${value} → Percentage = ${percentage}%`;
    result.classList.add("blue");
    box.style.borderColor = "var(--blue)";
  } 
  else if (value <= 100) { // Percentage case
    let cgpa = (value / 9.5).toFixed(2);
    result.innerText = `🎓 Percentage = ${value}% → CGPA = ${cgpa}`;
    result.classList.add("green");
    box.style.borderColor = "var(--green)";
  } 
  else {
    error.innerText = "⚠️ Enter a valid number (CGPA ≤ 10 or % ≤ 100)";
    box.style.borderColor = "var(--red)";
  }
}
</script>

</body>
</html>
